version: "2"

# One sqlc package per vertical slice: a slice owns its queries and its
# generated code, so adding a feature never edits another slice's files.
# Every package reads the same migrations directory for the schema.
sql:
  - engine: postgresql
    schema: migrations
    queries: internal/users/db/queries.sql
    gen:
      go: &gen
        package: usersdb
        out: internal/users/db
        sql_package: pgx/v5
        emit_pointers_for_null_types: true
        emit_empty_slices: true
        overrides: &overrides
          - db_type: uuid
            go_type: github.com/google/uuid.UUID
          - db_type: uuid
            nullable: true
            go_type:
              import: github.com/google/uuid
              type: UUID
              pointer: true
          - db_type: timestamptz
            go_type: time.Time
          - db_type: timestamptz
            nullable: true
            go_type:
              type: time.Time
              pointer: true

  - engine: postgresql
    schema: migrations
    queries: internal/auth/db/queries.sql
    gen:
      go:
        <<: *gen
        package: authdb
        out: internal/auth/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/conversations/db/queries.sql
    gen:
      go:
        <<: *gen
        package: conversationsdb
        out: internal/conversations/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/workouts/db/queries.sql
    gen:
      go:
        <<: *gen
        package: workoutsdb
        out: internal/workouts/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/jobs/db/queries.sql
    gen:
      go:
        <<: *gen
        package: jobsdb
        out: internal/jobs/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/media/db/queries.sql
    gen:
      go:
        <<: *gen
        package: mediadb
        out: internal/media/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/goals/db/queries.sql
    gen:
      go:
        <<: *gen
        package: goalsdb
        out: internal/goals/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/checkins/db/queries.sql
    gen:
      go:
        <<: *gen
        package: checkinsdb
        out: internal/checkins/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/memories/db/queries.sql
    gen:
      go:
        <<: *gen
        package: memoriesdb
        out: internal/memories/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/documents/db/queries.sql
    gen:
      go:
        <<: *gen
        package: documentsdb
        out: internal/documents/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/biometrics/db/queries.sql
    gen:
      go:
        <<: *gen
        package: biometricsdb
        out: internal/biometrics/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/calculator/db/queries.sql
    gen:
      go:
        <<: *gen
        package: calculatordb
        out: internal/calculator/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/activity/db/queries.sql
    gen:
      go:
        <<: *gen
        package: activitydb
        out: internal/activity/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/exercises/db/queries.sql
    gen:
      go:
        <<: *gen
        package: exercisesdb
        out: internal/exercises/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/meals/db/queries.sql
    gen:
      go:
        <<: *gen
        package: mealsdb
        out: internal/meals/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/preferences/db/queries.sql
    gen:
      go:
        <<: *gen
        package: preferencesdb
        out: internal/preferences/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/mind/db/queries.sql
    gen:
      go:
        <<: *gen
        package: minddb
        out: internal/mind/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/hydration/db/queries.sql
    gen:
      go:
        <<: *gen
        package: hydrationdb
        out: internal/hydration/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/sleep/db/queries.sql
    gen:
      go:
        <<: *gen
        package: sleepdb
        out: internal/sleep/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/habits/db/queries.sql
    gen:
      go:
        <<: *gen
        package: habitsdb
        out: internal/habits/db
        overrides: *overrides

  - engine: postgresql
    schema: migrations
    queries: internal/fitness/strava/db/queries.sql
    gen:
      go:
        <<: *gen
        package: stravadb
        out: internal/fitness/strava/db
        overrides: *overrides

